pipeline {
    agent any

    stages {
        stage('Sonar Analysis') {
            steps {
                echo 'Testing..'
                sh 'cd webapp && sudo docker run  --rm -e SONAR_HOST_URL="http://13.40.37.1:9000" -e SONAR_LOGIN="sqp_24f0ceced0b0efd4cb5a73e983cbad2d2ebc0fb1"  -v ".:/usr/src" sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms'
            }
        }


    }
}
